# 从“结果正确”到“访问路径正确”

SQL 描述想要什么，查询规划器选择怎样得到它。`EXPLAIN` 展示的是计划树：扫描节点产生行，上层节点进行过滤、连接、聚合或排序。

索引为特定谓词和排序提供另一条访问路径，但每个索引也会增加写入、空间和事务维护成本。工程判断应从真实查询形状出发，而不是把索引当作通用加速开关。

这个练习用 `unordered_set` 模拟等值索引。它不替代数据库，却能让“先构建访问结构，再执行查询”的边界变得可执行。
