# 线程、共享状态与原子操作

多个线程访问同一普通变量且至少一个线程写入时，缺少同步会产生数据竞争。`std::thread` 必须在析构前 join 或 detach；`std::atomic` 为简单计数提供明确的并发操作。

## 任务与检索

让四个线程各递增计数器一千次并逐个 join。运行前回答：为什么“多数时候输出正确”不能证明没有数据竞争？为什么 join 必须发生在读取最终计数之前？
