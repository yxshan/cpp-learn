# 算法库

标准算法把“如何处理元素”与“元素存在哪里”分开。大多数传统算法接收一对
迭代器表示半开范围 `[first, last)`；范围版本则直接接收范围。

## 使用路径

1. 明确输入范围和结果写入位置。
2. 检查算法要求的迭代器类别。
3. 核对复杂度、比较关系和是否会重排元素。
4. 用返回值判断查找位置或输出范围，而不是重复遍历。

## 常见入口

`std::find` 进行线性查找；`std::sort` 原地重排随机访问范围。两者都由
`<algorithm>` 声明。

## 边界

算法不会替容器修复失效的迭代器，也不会自动保证自定义比较器满足严格弱序。
