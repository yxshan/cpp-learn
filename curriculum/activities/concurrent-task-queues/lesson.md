# 并发组件的难点在关闭

启动工作线程通常很容易；真正决定组件可靠性的，是它如何停止。

一个有界任务队列至少需要回答：停止后是否还接收新任务？已接收任务是排空还是取消？阻塞中的 worker 如何被唤醒？谁负责 join？异常由谁保存和传播？

本机基线使用可移植的 `std::thread` 与显式 `join`；支持 `std::jthread` 的工具链可进一步加入协作停止，但两者都不会替你定义队列协议。本练习先建立最小生命周期：任务按值进入 worker，共享结果通过原子操作合并，主线程 join 后才读取结果。
