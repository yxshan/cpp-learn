# `std::vector::reserve`

`reserve(n)` 请求 `std::vector` 至少准备容纳 `n` 个元素的容量，但不会创建元素，
也不会改变 `size()`。

## 快速信息

- 头文件：`<vector>`
- 所属类型：`std::vector`
- 标准：C++98 起；C++20 起可为 `constexpr`
- 复杂度：最坏线性于当前元素数量

## 代表性声明

```cpp
constexpr void reserve(size_type n);
```

以上是 C++20 形式；更早标准中的成员不带 `constexpr`。

## 什么时候使用

当协议长度、批次数量或输入规模上界已经可估计时，可在批量追加前调用一次，减少中途
重新分配。若规模未知，不要为了“保险”在每次追加前线性增加请求容量；`reserve` 也不能
保护超过预留容量后的旧引用。

## 行为

若 `n > capacity()`，容器重新分配，调用后容量至少为 `n`。否则不会重新分配，
容量保持不变。调用不改变 `size()`。之后的插入在新大小超过容量前不会再次引起
重新分配，完整保证见 [`[vector.capacity]`](https://eel.is/c++draft/vector.capacity)。

## 返回值

`reserve` 返回 `void`。调用者不能从返回值获知是否发生重新分配，也不能假定实际
`capacity()` 恰好等于 `n`；需要观察容量时应在调用后读取 `capacity()`，并只依赖它至少
达到请求值这一合同。

## 复杂度

最坏情况下与调用前的 `size()` 成线性关系，因为重新分配时至多需要移动或复制全部现有
元素。若 `n <= capacity()`，不会发生重新分配；标准仍以最坏上界描述接口，不承诺某个
实现的分配增长因子或常数成本。

## 生命周期与失效规则

发生重新分配时，所有元素引用、指针、迭代器和尾后迭代器失效；没有重新分配时，
这些句柄保持有效。如果计划保存元素地址，应先预留足够容量，并确保后续大小不
超过容量。

## 异常与约束

`n > max_size()` 时抛出 `std::length_error`，分配失败也可以抛出分配器异常。
元素需满足移动插入要求。除不可复制元素的抛异常移动构造这一特殊情况外，异常
通常不改变 vector。

## 示例

第一个示例先证明 `reserve(3)` 后大小仍为零，再追加三个元素；它只断言
`capacity() >= 3`，因为标准不要求容量恰好等于请求值。第二个示例先预留三个字符串的
容量，在大小没有超过该容量时继续追加，并验证先前取得的元素指针仍指向首元素。

## 常见错误

`reserve()` 不是 `resize()`；对只预留容量的空 vector 使用 `values[0]` 仍然非法。
不要在每次 `push_back` 前调用 `reserve(size() + 1)`，这可能破坏容器原本的几何
增长策略，造成反复线性移动。只有能估计后续规模时才适合一次性预留。

## 与 JavaScript 的区别

> JavaScript `Array` 没有可移植的 `reserve`/`capacity` 接口，运行时自行管理内部存储；
> C++ `vector::reserve` 让调用者表达容量下界并直接影响地址稳定窗口。它仍不增加 `size()`，
> 也不会像 JS 数组赋值那样自动创建可访问元素。

## 相关内容

先阅读 `std::vector` 的整体模型和 `std::vector::push_back` 的追加规则。

## 来源

容量、复杂度、失效和异常规则依据由 Entry manifest 提供。
