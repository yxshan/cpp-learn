# 复制、移动与智能指针

值语义决定对象复制后的独立性；移动语义转移资源。`std::unique_ptr` 把唯一所有权写进类型系统。

本课用 `unique_ptr` 观察所有权转移：移动后，新对象负责资源，原指针仍可析构但不再拥有目标。接口若按值接收 `unique_ptr`，就是在声明“把所有权交给我”。

## 检索练习

在运行前画出移动前后的两个指针和一个堆对象。回答：为什么复制 `unique_ptr` 应当是编译错误，而移动是允许的？

常见误区：移动后继续解引用原指针，或把 `std::move` 理解成“立即搬运字节”；它只是允许选择移动语义，具体行为由类型决定。
