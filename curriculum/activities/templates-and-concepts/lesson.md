# 模板与 concepts

模板把算法从具体类型中抽离，concepts 让约束成为可读接口。实现接受两种可比较值的 `larger`。

模板不是运行时的“任意类型”；编译器会针对实际类型实例化代码。concept 把实例化所需的能力放在接口上，让不满足约束的调用在更靠近调用点的位置报错。

## 检索练习

不运行程序，判断 `larger(2, 7)` 与 `larger(2.5, 1.5)` 会产生几种实例化。再写出算法真正需要的操作，而不是列举允许的具体类型。

常见误区：认为模板只会生成一份机器码，或用一个宽泛 concept 掩盖实现其实需要的更具体操作。
