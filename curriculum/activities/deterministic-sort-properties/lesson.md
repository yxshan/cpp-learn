# 用性质验证排序

样例只能说明几个具体输入。性质测试描述一整类行为：输出必须非递减，并且保留输入中的全部元素和重复次数。Judge 使用固定 seed 生成整数序列；失败时记录 seed、caseIndex 与可复现反例。

## 任务

读取元素数量与整数序列，使用标准算法升序排序，并以空格分隔输出。先预测含重复值与负数时的输出，再提交 Grade 观察性质阶段。

## 检索练习

不看代码回答：仅验证“输出有序”为什么不足以证明排序正确？seed 在排查随机失败时承担什么角色？

常见误区：把集合相等误写成忽略重复次数，或每次失败都使用新 seed，导致反例无法复现。
