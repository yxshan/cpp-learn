# Review：解释增长比

这次不重做上一题。先闭卷写下增长比公式，再预测 `10 20` 和 `10 40` 会落在哪一侧。

## 任务

程序读取 baseline 与 scaled 两个中位耗时。计算 `scaled / baseline`：不超过 3.2 输出 `within-boundary`，否则输出 `over-boundary`。

完成后解释：同机、交替运行、重复取中位数减少了哪些噪声？为什么这仍是工程证据，而不是复杂度的数学证明？

## 检索步骤

先独立作答。只有无法解释“为什么不使用绝对毫秒阈值”时，才回看参考资料中关于测量方差的段落。
