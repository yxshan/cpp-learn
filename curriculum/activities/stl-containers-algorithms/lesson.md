# STL 容器、迭代器与算法

容器负责存储，迭代器描述范围，算法表达操作。读取三个整数放入 `vector`，排序后输出。

把 `vector` 看作拥有连续元素的资源对象，把 `[begin, end)` 看作算法处理的半开区间。`std::sort` 不关心容器名字，只通过迭代器读取和重排范围中的元素。

## 检索练习

先预测三个相等元素排序后的结果，再解释为什么 `end()` 不指向最后一个元素。尝试只改数据数量，判断程序中哪些部分需要随之变化。

常见误区：解引用 `end()`、手写循环时越界，或在容器结构变化后继续使用已经失效的迭代器。
