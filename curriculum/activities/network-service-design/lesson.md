# 服务不是 `read()` 一次就结束

TCP 交付的是有序字节流，不承诺一次 `read` 对应一次请求。服务必须自己定义消息从哪里开始、在哪里结束，以及最多允许占用多少内存。

## 三层契约

1. 传输层负责字节抵达和顺序。
2. 消息层用长度或分隔符恢复完整帧。
3. HTTP/业务层解释 method、target、状态码和内容。

长度字段不能被盲目信任。先检查上限，再等待完整载荷；当生产速度高于消费速度时，应暂停读取、拒绝任务或限制队列，而不是无限缓存。这就是背压的工程意义。

本练习把协议问题缩成一个纯边界：声明长度必须等于实际长度，并且不得超过服务上限。
