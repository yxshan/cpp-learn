# Loopback Socket 与最小 HTTP 往返

本实验把服务端绑定到 `127.0.0.1` 和端口 `0`：操作系统分配动态端口，客户端只在同一进程内连接它。服务线程读取请求并返回最小 HTTP 响应，整个过程不访问外网，也不争抢固定端口。

## 检索练习

解释 `INADDR_LOOPBACK`、动态端口、`listen/accept` 和客户端 `connect` 的先后关系。为什么测试不应绑定一个写死的端口？
