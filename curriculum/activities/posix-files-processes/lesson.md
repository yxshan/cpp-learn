# 文件与子进程生命周期

系统代码不仅要得到正确结果，还要明确每个资源由谁关闭、回收和删除。本实验在 Judge 的单次临时目录中写入文件，`fork` 子进程读取内容并通过退出状态返回字节数，父进程用 `waitpid` 回收子进程，最后删除文件。

## 检索练习

先画出父进程、子进程、文件描述符与临时文件的生命周期。若省略 `waitpid` 或文件清理，哪类资源会留到进程结束以后？

本实验使用 POSIX 接口，可在 macOS/Linux 本地验证；它不产生仅 Linux 才成立的结论。
