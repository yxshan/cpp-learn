# `std::array`

`std::array<T, N>` 保存恰好 `N` 个 `T`，把固定大小的连续元素序列包装成标准容器。

## 快速信息

- 头文件：`<array>`
- 命名空间：`std`
- 标准：C++11 起
- 存储：连续，大小不变

## 代表性声明

```cpp
namespace std {
template<class T, std::size_t N>
struct array;
}
```

`std::array<int, 3>` 和 `std::array<int, 4>` 是不同类型，因为 `N` 是模板参数。

## 什么时候使用

元素数量属于类型或协议本身，例如固定坐标、颜色通道、阶段表或小型查找表时使用。
若数量来自运行时输入或需要增长，应使用 `std::vector`；若只需要 C 风格数组且不需要
标准容器接口，也不必为了形式统一强行包装。

## 复杂度

元素访问、迭代器算术和 `size()` 为常数复杂度。`fill()` 和逐元素 `swap()` 对
`N` 为线性复杂度。容器不分配可增长的动态存储，也不会发生扩容。

## 生命周期与失效规则

容器大小和存储位置不会因成员操作重新分配，指针、引用和迭代器在对应
`std::array` 对象的生命周期内仍关联该对象。交换两个数组会交换位置中的元素值，
已有迭代器不会改为指向另一个容器。

## 元素访问与异常

`at(n)` 在 `n >= size()` 时抛出 `std::out_of_range`。`operator[]` 要求索引有效；
`front()` 和 `back()` 要求容器非空。对于 `N == 0`，`begin() == end()`，且不能
访问元素，`data()` 的返回值也未指定，详见
[`[array.zero]`](https://eel.is/c++draft/array.zero)。元素自身的构造、赋值、比较或
交换异常会按相应操作传播。

## 初始化

优先使用花括号初始化。对于标量元素，`std::array<int, 3> values{};` 会清晰地进行
值初始化；不要误以为默认初始化总会把整数清零。

## 示例

第一个示例读取数组的固定大小、首元素和尾元素。第二个示例用
`std::array<std::string_view, 3>` 表达固定的构建流水线阶段，并按索引读取测试阶段。

## 常见错误

需要增长时不要强行选择 `std::array`。也不要对 `std::array<T, 0>` 调用
`front()`、`back()` 或下标访问。

## 与 JavaScript 的区别

> JavaScript `Array` 的长度可在运行时增长，元素类型也不进入容器的静态类型；
> `std::array<T, N>` 把元素类型和固定数量 `N` 都写入类型。JS `TypedArray` 在“固定长度、
> 连续数值存储”上更接近，但其底层缓冲区由运行时对象管理，也没有 C++ 迭代器失效与对象
> 生命周期合同。

## 相关内容

`<array>` 声明该类型；`std::vector` 适合运行时大小的连续序列。

## 来源

固定大小、复杂度、存储和零长度规则依据由 Entry manifest 提供。
