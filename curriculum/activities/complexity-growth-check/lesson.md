# 用增长比观察复杂度

绝对耗时会随机器、负载和编译器变化。相对性能检查在同一可执行文件、同一临时环境中交替运行基线与放大输入，取多次运行的中位数，再比较增长比。

## 任务

程序输出从 `0` 到 `n-1` 的和，同时执行一段与 `n` 成线性关系的工作。公开测试验证结果，性能阶段验证输入翻倍时中位耗时不会呈平方增长。

## 检索练习

为什么不能把“必须在 20ms 内完成”当作跨机器能力证据？中位数比单次耗时更能抵抗什么干扰？
